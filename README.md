# priority-encoder
a combinational circuit that converts multiple input signals into a binary code based on the highest-priority active input. It is commonly used in digital systems for efficient data selection and processing.
